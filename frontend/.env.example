# Base URL for the roles REST API.
# Leave unset in local dev to use the Vite proxy ('/api' -> http://localhost:3001).
# In production, set this to the deployed backend's base URL, e.g. https://api.example.com/api
VITE_API_URL=
